
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Dependencies
node_modules/

# Expo
.expo/
dist/

# Native
# ios/ and android/ are intentionally committed and hand-maintained (built via
# Xcode / Android Studio / Gradle directly, not EAS). ios/ contains the
# hand-written FleetToolSiriIntents.swift App Intent and Info.plist/entitlements
# changes; android/ contains a manual fix to styles.xml (see commit history).
# Do NOT run `expo prebuild --clean` without first confirming those files are preserved.

# Build artifacts
*.jsbundle

# Android build outputs / local machine config (android/ source itself is committed)
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/*.hprof

# Environment variables
.env
.env.local
.env*.local

# macOS
.DS_Store

# Editor
.vscode/
